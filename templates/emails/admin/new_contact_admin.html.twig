{% extends 'emails/layout_admin.html.twig' %}

{% block content %}
    <p>
        referer : {{ referer }}
    </p>
    <p>
        token : {{ token }}
    </p>
    <p>
        Temps pour soumettre le formulaire : {{ time }}
    </p>

    <p>De : {{firstName}} {{lastName}}</p>
    <p>Du camper : {{username}}</p>
    <p>Email : {{emailContact}}</p>
    <p>Contenu :</p>
    <p>{{ content }}</p>
    <br />
    <a href="https://gardenbivouac.org/fr/admin">Aller sur Garden Bivouac répondre</a></p>

{% endblock %}
