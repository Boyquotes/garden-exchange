{% extends 'emails/layout_email.html.twig' %}

{% block content %}

    <img src="{{ email.image('@images/logo-garden-bivouac.png') }}" alt="Logo Garden Bivouac">
    <h1>{{ 'email.new.conversation'|trans }}</h1>

    <p><a href="{{ path('homepage') }}">{{ 'email.garden.near'|trans }} {{ garden.city }}</a></p>

{% endblock %}
