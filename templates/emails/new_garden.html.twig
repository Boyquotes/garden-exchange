{% extends 'emails/layout_email.html.twig' %}

{% block content %}

    <img src="{{ email.image('@images/logo-garden-bivouac.png') }}" alt="Logo Garden Bivouac">
    <h1>{{ 'email.new.garden'|trans }}</h1>

    <p>
        {{ 'email.the.garden'|trans }}
    </p>
    <p>{{ 'email.garden.near'|trans }} {{ garden.city }}</p>

{% endblock %}
