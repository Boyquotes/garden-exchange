{{ form_start(searchForm, {'action': path('homepage'), 'method': 'POST'}) }}
    {{ form_row(searchForm.where) }}
    <button class="btn btn-primary"><i class="fa fa-search" aria-hidden="true"></i></button>
{{ form_end(searchForm) }}
