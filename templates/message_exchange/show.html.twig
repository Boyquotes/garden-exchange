{% extends 'base.html.twig' %}

{% block title %}MessageExchange{% endblock %}

{% block body %}
    <h1>MessageExchange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message_exchange.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ message_exchange.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_exchange_index') }}">back to list</a>

    <a href="{{ path('message_exchange_edit', {'id': message_exchange.id}) }}">edit</a>

    {{ include('message_exchange/_delete_form.html.twig') }}
{% endblock %}
