{% extends 'base.html.twig' %}

{% block title %}{{ 'user.reset.password'|trans }}{% endblock %}

{% block body %}
    <div class="well">
        {{ form_start(resetForm) }}
            <legend><i class="fa fa-user" aria-hidden="true"></i>{{ 'user.reset_password'|trans }}</legend>
            {{ form_row(resetForm.password) }}
            <button class="btn btn-primary">{{ 'user.reset_password'|trans }}</button>
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
