{{ form_start(form) }}
    {{ form_errors(form.content) }}
    {{ form_widget(form.content,{ 'attr': {'rows': '10', 'cols': '50'} }) }}
    <button class="btn btn-primary pull-right send-message">{{ button_label|default('send.message'|trans) }}</button>
{{ form_end(form) }}
