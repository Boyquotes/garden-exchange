homepage:
    path: /{_locale}
    controller: App\Controller\HomepageController::homepage
    requirements:
        _locale: '%app_locales%'
    defaults:
        template: default/homepage.html.twig
        _locale: '%locale%'
